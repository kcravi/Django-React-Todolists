# Django-React-Todolists

A simple Todolist app built with DJANGO and REACT-REDUX. The backend uses Django Rest Framework to host a simple API. The frontend uses React, Redux and queries data from the API.
